phase-F checkpoint after successful synthesis before shape rebuild

Trident standing order and compaction handoff

Work on the real Windows machine in C:\Users\eb-wjt\Downloads\3-way\Trident, PowerShell only. No bash, python3, or &&. Use python or .\.venv\Scripts\python.exe. Branch reduction; origin https://github.com/wgabrys88/Trident.git. Preserve unrelated untracked desktop.ini. Do not spawn agents unless asked.

Checkpoint control
The user requested a full-message commit, push, and pause before the next local build for compaction. This checkpoint satisfies that boundary. Resume when the user asks: read git log -1 --format=%B and this entire file, then perform the pending rebuild and remaining work. Do not repeat the pre-build pause for the already committed changes. This is NOT Phase F completion. No active build or tool session remains. Leave the resident server alone until EngineBuild kills it for the pending rebuild.

Mission
python tts.py nano|turbo|v3 [flags] TEXT [language] downloads checkpoints, converts GGUF, bakes a voice, reuses or starts one detached named-pipe Vulkan server, writes one WAV, and prints its path. At most one chatterbox-server-*.exe on the machine. Same command+voice contract reuses VRAM; a changed contract kills the previous server before starting another. Keep GPT-2 nano/turbo meanflow and Llama v3 CFG as separate libraries, S3/T3 graphs, bakers and server binaries. GPT-2 bake conditioning is 15 seconds; Llama is 6 seconds. Llama removes its final 960 samples before shared Audio::fade. Product output is 24 kHz, not a user flag or competing GGUF output-rate key.

Completed implementation
- [x] Phase A, 45a5636: one models/server.pid. PipeServer.ensure migrates other models/*.pid via kill, compares command+voice, probes matching pipe for 1000 ms, reuses or kills then Popen with DETACH and DEVNULL. Startup exit/timeout throws. kill uses taskkill /F /T /PID, waits, unlinks. No host TerminateProcess, siblings, or atexit. Build/bake kill server.pid before replacing locked files.
- [x] Phase B, c8bff24: settings.FLAGS owns names/defaults/help/groups/architecture. Generated argparse and exact host pass-through. C++ mandatory flags, no defaults. gpu=0 reaches VulkanBackend(device); failure throws. Build contract hashes CMakeLists.txt plus ggml revision/generator/architecture; CMAKE_FLAGS removed. Llama context uses GGUF layout plus n-predict; no converted n_ctx/+4096. GPT-2 context is wpe length. Speech caps come from GGUF.
- [x] Phase C, d93f30c: quant.py Policy(default,rules), TYPES from GGMLQuantizationType; integer tensors remain integer, first matching conjunctive rule wins, packing errors propagate. No quant fallbacks. T3Converter moved into convert_t3.py; quant_policy.py and precision_policy.json deleted. Converters require type/policy args and use policy.add. Host conversion/bake contracts store resolved default+rules. Names contain default plus canonical rules SHA256[:8]. GgufFile::floats requires F32. Native Vulkan weights and F16 3D convolution expansion retained.
- [x] Phase D, ed6bfcb: root/variant help from table, Model/GGUF conversion/Server groups, enum types, integer preservation, JSON mixing, restart/reconvert explanation. Tokenizer paths private host-filled arguments.
- [x] Phase E, 663337f: shared repeat_penalty.h and Audio::fade, Llama tail retained; BatchNorm narrating comment removed. Checkpoints e9c3fb3 and f73d1f6 preserve earlier build work.
- [x] f73d1f6 shape corrections: bakers derive speaker length/mel width; VE dimensions from ve.safetensors (proj.weight); S3 dimensions/conformer counts/head geometry/feed-forward size/tokenizer dimensions and codebook/CampPlus embedding and dense counts from checkpoint. Tokenizer mel filters from checkpoint. S3 graphs use file mel/input/speaker sizes, conformer geometry, CFM widths, residual channels, time embedding widths, HiFT upsample sizes. Converter head geometry constants live in metadata rather than duplicated C++ constants. Shared T3Converter.main replaces duplicate CLI glue. Added missing <string> for MSVC std::to_string.

Runtime failure resolved in this checkpoint
The previous empty pipe acknowledgement was caused by a Vulkan assertion in ggml-vulkan.cpp:12594 rejecting quantized operands for an ordinary elementwise operation. Reproduced with the exact server.pid command in foreground and existing stderr, without adding logging or tests. Actual S3 GGUF attention biases flow/encoder/*/attn/pos_bias_u and pos_bias_v had shape [64,8], type Q4_0, and both separate S3 graphs use them directly in ggml_add. Added an explicit shipped S3 rule {"contains":"/attn/pos_bias_","type":"f32"}. This triggers new S3 filename/contract, conversion, bake, restart. Nano, turbo, v3 then synthesize successfully. No engine fallback or CPU dequantization was added. Old failing S3 file with rules hash 4b9488a1 remains unused on disk.

Actual validation with existing compiled binaries
- [x] All four binaries previously built successfully on this machine: GPT-2/Llama servers and bakers, separate libraries. MSVC 19.44, Windows SDK 10.0.26100.0, Vulkan SDK 1.4.357.0. Vulkan ON; CPU/CUDA/OpenMP OFF. ggml HEAD 7840aaba1989c6deeefede1d77d5aaf8f52b947e. No pip installs, requirements or ggml edits.
- [x] Root/nano/v3 help previously ran successfully with expected groups/defaults and --gpu 0.
- [x] Nano default synthesis: 54-17-09-20-09-26_nano.wav, 24 kHz, 1.56 s, PID3016.
- [x] Same nano contract reused PID3016: 23-18-09-20-09-26_nano.wav, 1.92 s. Exactly one server in snapshot.
- [x] Nano --seed 43 replaced PID3016 with PID3048: 33-18-09-20-09-26_nano.wav, 2.60 s. Exactly one server.
- [x] Turbo default replaced nano with PID10044: 42-18-09-20-09-26_turbo.wav, 1.44 s. Exactly one server.
- [x] V3 default English replaced turbo with PID9632: 13-19-09-20-09-26_v3.wav, 2.08 s. Exactly one server; tokenizer Python children expected.
- [x] Nano --t3-weight-type q8_0 converted, baked, replaced v3 with PID180: 05-20-09-20-09-26_nano.wav, 2.28 s. GGUFReader confirms model/h0/attn/c_attn/w and model/h1/attn/c_attn/w are Q8_0 in chatterbox-t3-nano-q8_0-297be1e6.gguf.
- [x] JSON layer override: ignored local models/quant_t3_layer0.json contains {"prefix":"model/h0/","type":"f32"} prepended to shipped T3 rules. python tts.py nano --t3-quant-policy models/quant_t3_layer0.json 'Layer zero uses full precision.' converted, baked, started PID7520 and wrote 21-21-09-20-09-26_nano.wav, 2.40 s. chatterbox-t3-nano-q4_0-182b868d.gguf has layer0 F32, layer1 Q4_0. Default chatterbox-t3-nano-q4_0-297be1e6.gguf has both Q4_0.
- [x] Current baked S3 has flow/encoder/block0/attn/q/w Q4_0 and pos_bias_u F32. WAV rates/durations inspected from actual artifacts, not golden files or tests. All named WAVs are in repository root, ignored.
- [x] git diff --check passed before checkpoint, only CRLF normalization warnings.
- [ ] New C++ changes below are NOT built or runtime-verified. Existing binaries used for all synthesis evidence predate these changes.

Pending source changes committed here
Weights::count(prefix,suffix) counts actual uploaded tensor names. Both separate S3 implementations use it for CFM attention-stack count, CFM mid-block count, pitch convolution count, and HiFT residual convolution count. Source harmonic count derives from source_weight_.size(). HiFT residual kernels derive independently from convs1/convs2 tensor shapes, removing source/residual kernel arrays. Audio::kaldi derives mel channels from filter element count / 257 instead of literal 80. These need the next build and real synthesis validation.
models/build-contract.json WAS DELETED intentionally, so the next python tts.py invocation rebuilds. EngineBuild removes intermediate build files after success, retains build/bin; expect shader regeneration. Build contract does not hash source files. Conversion contracts likewise do not hash converter source, so invalidate relevant conversion+bake stamps if changing metadata without changing policy. No conversion-source change was made in this checkpoint.
Current models/server.pid is valid for nano custom layer0 policy, PID7520 at last observation. Leave it resident; next EngineBuild.ensure kills it. No executor sessions remain running. Preserve the ignored custom policy for repeat validation. Old precision1 and earlier GGUFs remain untouched.

Remaining Phase F
- [ ] Rebuild all four targets using the already invalidated build stamp. The user-requested pre-build checkpoint is complete; resume directly with build, no repeated pause/permission request for these changes.
- [ ] Run actual nano and v3 synthesis after the C++ rebuild (and turbo if shape changes warrant), verify one server, same-contract reuse and contract-change replacement remain correct. Keep final server resident.
- [ ] Finish strict shape/metadata audit. Remaining architecture versus tensor facts include HiFT upsample/residual group counts and padding, CampPlus feature metadata/filter generation (still converter literal80), CampPlus head block counts, and converter head geometry constants. CFM stack/mid counts just fixed in source. Do not blindly treat FFT/stride/special-token slots as weight shapes. Remove unused metadata rather than competing sources. No CLI-owned sampling authority in GGUF.
- [ ] Investigate historical crash snapshot with PIDs3064 and9300 from f73d1f6: later both vanished. Foreground reproduction now exposed Vulkan assertion, and every successful synthesis snapshot showed exactly one server. Cause of the historical second transient process remains unproven; do not invent explanation or claim continuous process monitoring was done.
- [ ] Finish net first-party line audit relative to 2adbad0, excluding this prompt and allowed new help strings. Last pre-checkpoint baseline was +25 lines; this session adds approximately13 more. Requirement is net decrease apart from help. Reduce genuine duplicate glue, not cosmetic line compression. Do not claim this is satisfied yet.
- [ ] Final static audit: requirements unchanged, ggml and sibling untouched; no force_f32, siblings, CMAKE_FLAGS, precision_policy.json or host TerminateProcess; existing Llama tokenizer TerminateProcess is unrelated. No tests, pip installs, CPU backend, watermark or merged graphs. quant.py and two shipped JSONs remain the entire quant mechanism.
- [ ] Only when every item is complete, rewrite this entire file as maintenance standing order with empty work checklist, commit full-message and finish. Never mark Phase F complete just because runtime succeeded before latest source edits.

Permanent SST ownership
CLI owns seed, temperature, GPT-2 top-k, top-p, repeat-penalty, n-predict, cfm-steps, trim-fade-samples, Llama min-p/cfg-weight/exaggeration/cfm-cfg, gpu; t3/s3 weight type and policy; variant/TEXT/language/reference. Defaults exclusively settings.FLAGS: seed42, temperature.8, top-k1000, repeat1.2, n-predict1000, trim480, gpu0; GPT-2 top-p.95/cfm2; Llama top-p1/cfm5/min-p.05/cfg.5/exaggeration.5/cfm-cfg.7; q4_0 defaults, shipped JSONs, reference.wav. Host exact --names, C++ requires all without defaults. A new flag requires one table row, generated help/pass-through, Flags read, Knobs field, engine use. CMakeLists.txt alone owns compile switches.
GGUF owns tensor shapes, head/layer/FF/embedding sizes, perceiver/text/wpe layout, tokenizer tokens/IDs/languages, baked tensor lengths, and stored per-tensor types. No CLI sampling authority in GGUF, no +4096 Llama context budget, no ignored competing output sample rate.

Quant contract
Policy(default,rules). Integer arrays remain integer. First conjunctive prefix/suffix/contains/ndim match wins, else default. TYPES are lowercase GGMLQuantizationType names. f32/f16/bf16 and packed types go through gguf.quants.quantize with errors propagated. No Q4_K_M recipe, imatrix, quant catch/retry/f16 fallback, or CPU Q4 dequant. Native Vulkan upload retained; F16 3D conv expansion retained. GgufFile::floats rejects nonF32.
T3 F32: prefixes chatterbox/, voice_encoder/, model/wpe, model/ln_f/, model/norm/, model/rope_freq_factors; suffix /b,/g; ndim1. S3 F32: prefixes flow/input_embedding, flow/spk_embed_affine/, s3gen/builtin/, s3gen/mel_fb/, campplus/, s3tokv2/, cfm/, hift/; contains /attn/pos_bias_; suffix /b; ndim1,3,4. Other matrices default q4_0. JSONs own rules, settings owns default. Changing rules or default reconverts/rebakes/restarts. Filenames are chatterbox-t3-{variant}-{default}-{sha8}.gguf and chatterbox-s3gen-{family}-{default}-{sha8}.gguf where sha8 hashes canonical rules JSON.

Prohibitions
No tests, pytest, harnesses, CI, log files, debug prints, golden WAVs, scratch scripts, README, or extra markdown. No defensive fallbacks or swallowed errors. No pip installs or requirements edits; language packages pykakasi, spacy-pkuseg, dicta-onnx, add-stress-to-epub stay. Do not add huggingface_hub/scipy/perth/onnxruntime. Torch conversion stays CPU via PYTORCH_CPU_INDEX. Do not edit ggml/ or C:\Users\eb-wjt\Downloads\3-way\chatterbox.cpp. No watermark/Perth, --n-gpu-layers, --n-threads, CPU ggml backend, or merged GPT-2/Llama engines/bakers/servers. No unauthorized messages to others or unnecessary permission questions.

Commit ritual
After every phase including F: read this file in full; rewrite ENTIRE file from zero with mission, actual completed/remaining work and discoveries. First line is git subject, blank line, full standing order. Stage implementation and this file; git commit -F PROMPT-BOOTSTRAP-POSTERITY.md. No short dummy message, append-only changelog, or premature maintenance status. Continue next phase unless the user requested pause. This checkpoint must be pushed to origin/reduction and paused before build. Resume sentence: Read git log -1 --format=%B and PROMPT-BOOTSTRAP-POSTERITY.md in full, then resume all remaining work from the committed checkpoint.
