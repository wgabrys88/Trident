phase-0: implement Trident exclusive-server + SST flags + removable quant — do not stall

Trident standing order

Work in C:\Users\eb-wjt\Downloads\3-way\Trident on this Windows machine. PowerShell only. No bash, python3, or &&. Use python or .\.venv\Scripts\python.exe. Branch reduction; origin https://github.com/wgabrys88/Trident.git. Preserve untracked desktop.ini and Trident.code-workspace. Do not spawn agents unless asked. Edit live files. Build here. Do not recap instead of working.

Goal
python tts.py nano|turbo|v3 [flags] TEXT [language] downloads checkpoints, converts GGUF, bakes a voice, reuses or starts one detached named-pipe Vulkan server, writes one 24 kHz WAV, and prints its path. At most one chatterbox-server-*.exe. Same command+voice contract reuses VRAM; any other contract kills the live server then starts the new one. Exclusive server, one flag-default table, and removable JSON quant are the product, not a side quest. v3 is multilingual Llama: the positional language argument is a real user flag, the MTL tokenizer child must receive it, and the GGUF must record the tokenizer language-token inventory. Do not stall on re-planning A–E; they are already on this branch. Finish the remaining product proofs and the F2 tokenizer-fact regression, then stop.

Execute rules
No tests, pytest, harness, CI, log files, debug prints, golden WAVs, scratch scripts, README, or extra markdown besides this file. No defensive fallbacks. No swallowed errors. No CPU ggml path. No silent f16 when a quant cannot pack. Raise or throw. No narrating comments. Do not edit ggml/ or C:\Users\eb-wjt\Downloads\3-way\chatterbox.cpp. Do not pip install. Do not change requirements.txt; keep pykakasi, spacy-pkuseg, dicta-onnx, add-stress-to-epub for v3. Do not add huggingface_hub, scipy, perth, onnxruntime. Torch conversion stays CPU via PYTORCH_CPU_INDEX. CMake: GGML_VULKAN ON, GGML_CPU OFF, GGML_CUDA OFF, GGML_OPENMP OFF. Do not add --n-gpu-layers, --n-threads, watermark, Perth, or a CPU backend. Do not merge GPT-2 and Llama S3, T3, baker, or server binaries.

SST — two owners, never both
CLI-owned (one table in settings.py: name, default, help, group model|conversion|server, architecture gpt2|llama|both). argparse and host --name pass-through are generated from that table. C++ Flags read those exact names; missing flag throws; zero defaults in C++ or knobs.h. Convert receives --matrix-type/--weight-type and --quant-policy from the host; no convert default when flags are omitted.
CLI: seed, temperature, top-k (gpt2), top-p, repeat-penalty, n-predict, cfm-steps, trim-fade-samples, min-p/cfg-weight/exaggeration/cfm-cfg (llama), gpu; t3/s3 weight type and policy; variant, TEXT, language (v3), --reference.
Table defaults: seed 42, temperature 0.8, top-k 1000, repeat-penalty 1.2, n-predict 1000, trim-fade-samples 480, gpu 0; GPT-2 top-p 0.95 / cfm-steps 2; Llama top-p 1.0 / cfm-steps 5 / min-p 0.05 / cfg-weight 0.5 / exaggeration 0.5 / cfm-cfg 0.7; q4_0 and shipped JSON policies; reference.wav. A new flag is one table row plus generated help, host pass-through, Flags read, Knobs field, engine use. v3 tokenizer filesystem paths stay host-filled, not user flags, not in help.
GGUF-owned (facts of this file, never CLI, never a drifting C++ literal): tensor-derived layout (n_embd, n_head, n_layer, n_ff, embedding sizes, perceiver length, text position length, GPT-2 wpe length); tokenizer facts (GPT-2 token lists/merges, start/stop ids, Llama language-token list); baked tensors and their lengths; per-tensor ggml dtype. Engine never reads GGUF to decide sampling. No chatterbox.temperature, s3gen.cfm_steps, n_predict, or +4096 n_ctx budget in GGUF. Llama runtime context is GGUF layout plus CLI n-predict plus the graph’s fixed special-token slots. GPT-2 context is wpe. Speech vocab/start come from the file or its tensors, not 6561. Architecture that is not a user flag and not a weight shape stays architecture: 15 s GPT-2 / 6 s Llama bake conditioning, Llama 960-sample tail then Audio::fade, 24 kHz product output. Do not add those as CLI. Do not store a competing sample-rate key that Audio::write ignores.
One authoritative source per fact the engine reads. If C++ already takes a shape from a tensor, do not also require a competing kv for that same number. Tokenizer language tokens are not a tensor shape: they belong in GGUF as tokenizer.language_tokens even when the runtime encoder is the checkpoint JSON. Do not treat unread encoder-geometry kv (attention_heads, ff_size, token_mel_ratio, lookahead, cfm.head_dim, mlp_ratio) as something to restore.

Quant — separate and removable
scripts/quant.py is Policy(default, rules).add only. TYPES from lowercase GGMLQuantizationType names. Integers stay integer. First conjunctive prefix/suffix/contains/ndim match wins, else default. f32/f16/bf16 and packed types go through gguf.quants.quantize; packing errors propagate. No QuantError catch, f16 retry, Q4_K_M recipe, imatrix, or CPU Q4 dequant. Native Vulkan upload retained. F16 3D conv expansion retained. GgufFile::floats requires F32.
Shipped T3 f32: prefixes chatterbox/, voice_encoder/, model/wpe, model/ln_f/, model/norm/, model/rope_freq_factors; suffix /b,/g; ndim 1. Shipped S3 f32: prefixes flow/input_embedding, flow/spk_embed_affine/, s3gen/builtin/, s3gen/mel_fb/, campplus/, s3tokv2/, cfm/, hift/; contains /attn/pos_bias_; suffix /b; ndim 1,3,4. CLI type overrides JSON default; --t3-quant-policy / --s3-quant-policy replace rules. Host conversion contract stores resolved {default, rules}. Names: chatterbox-t3-{variant}-{default}-{sha8}.gguf and chatterbox-s3gen-{family}-{default}-{sha8}.gguf. quant.py plus scripts/quant_t3.json and scripts/quant_s3.json are the whole mechanism.

Exclusive server
One models/server.pid. wanted = {command, voice} with exe, t3, s3, pipe, and every server flag. Migrate leftover models/*.pid. Matching contract and WaitNamedPipeW(pipe, 1000) reuses. Else kill, Popen DETACH, DEVNULL, wait for pipe; process exit or timeout throws. kill is taskkill /F /T /PID, wait, unlink. EngineBuild and VoiceBake kill server.pid before replacing locked files. No host TerminateProcess, siblings, or atexit. Llama tokenizer child TerminateProcess is unrelated. Do not kill the server when tts.py exits.

Architecture already in the graphs
nano/turbo = GPT-2 meanflow, 15 s bake cond. v3 = Llama CFG, 6 s bake cond, multilingual tokenizer. Shared bake_voice in src/common/bake.h; wrappers stay 15 s and 6 s; binaries stay split. HiFT ups/rate/pad and residual groups come from tensor counts (rate=kernel/2, source stride=max(kernel/2,1), pad=(stride-1)/2). CampPlus n_mels and head/xvector counts come from weights. S3 width/mels/speaker/vocab and encoder blocks come from tensors / Weights::count. CFM head from pos_bias_u->ne[0]. Llama speaker2 = 2*mels_. Llama heads from n_embd/(2*rope_freq_factors). Do not merge graphs to save lines.

Already true on reduction at ff52ab5
Phases A–E are committed: exclusive server, SST table, removable quant, grouped help, shared repeat_penalty/fade, CMAKE_FLAGS/precision_policy/siblings/force_f32 gone. F2 shape/baker work is committed. First-party vs 2adbad0 excluding this file and ggml is 494 insertions / 499 deletions (net -5). That decrease must survive remaining edits except new help strings.
Verified on this machine after the F2 binaries (10:19 20 Sep 2026, MSVC 19.44.35228.0, Vulkan SDK 1.4.357.0, ggml 7840aaba): nano 39-14-10-20-09-26_nano.wav 24 kHz 2.04 s PID9836; reuse 10-20-10-20-09-26_nano.wav same PID ~4.1 s; turbo 18-20-10-20-09-26_turbo.wav 24 kHz 1.84 s PID13204. Exactly one server at each successful snapshot. Resident server at this rewrite is PID13204 turbo; leave it until the next contract change. v3 convert/bake stamps for 297be1e6/94058b8f are absent. Layer0 182b868d convert.json is absent. precision1, 4b9488a1, and models/quant_t3_layer0.json remain. Historical two-PID snapshots stay unproven. English-only F1 v3 and pre-F2 q8_0/layer0 do not count for current source.

Remaining work — do this next, in order, without a new design phase
- [ ] Restore Llama GGUF tokenizer facts F2 deleted: write chatterbox.tokenizer.language_tokens from MTL vocab tokens matching \[[a-z]{2,3}\]; write n_ff from gate_proj.weight. Language remains CLI. After that converter change, delete only active v3/layer0 convert/bake stamps that would skip (v3 stamps are already gone). Leave precision1 and 4b9488a1.
- [ ] Replace GPT-2 speech pad literal 4299 with a GGUF/tensor fact (same SST rule as the old 6561 cap). Do not invent a CLI for it.
- [ ] python tts.py nano -h and python tts.py v3 -h show Model / GGUF conversion / Server from the one table. Defaults match the table including --gpu 0. v3 help includes language. Tokenizer paths absent from help.
- [ ] Prove current binaries+GGUFs: v3 English; v3 with a second language that exercises the MTL child (ja, zh, he, or ru). Then default nano reuse or replace as needed so a snapshot after nano then turbo then v3 shows exactly one chatterbox-server-*.exe. Same flags reuse. Flag or model change unloads first.
- [ ] python tts.py nano --t3-weight-type q8_0 TEXT reconverts, rebakes, restarts. python tts.py nano --t3-quant-policy models/quant_t3_layer0.json TEXT pins layer 0 with no code change.
- [ ] git diff --stat 2adbad0 HEAD excluding this file and ggml remains a net first-party decrease except allowed new help. Restore language_tokens/n_ff by adding facts, not by duplicating bake/S3 glue.
- [ ] Static close: requirements.txt unchanged; ggml and sibling untouched; no force_f32, siblings, CMAKE_FLAGS, precision_policy.json, host TerminateProcess, tests, extra pip, CPU backend, watermark, or merged graphs; quant.py + two shipped JSONs remain the whole quant mechanism.
- [ ] Only then rewrite this entire file as a maintenance standing order with the same mission/SST/quant/server/prohibitions/ritual and an empty work checklist. Commit -F. Stop. Never mark the goal complete because nano/turbo succeeded while v3, multilingual GGUF facts, q8_0, or layer0 are unverified on current source.

Commit ritual
Read this file in full. Rewrite the entire file from zero: first line is the git subject, blank line, then the standing order with actual completed vs remaining work. Stage this file with any implementation. git commit -F PROMPT-BOOTSTRAP-POSTERITY.md. Push origin/reduction. Continue the remaining list immediately unless the user asked to pause. No dummy subject, no append-only changelog, no empty-checklist maintenance status while work remains.

Do not
Plan-only replies, permission questions, sandbox trees, or restating this file instead of editing. Tests, harnesses, loggers, extra markdown. Fallbacks, C++ knob defaults, convert weight-type defaults when the host omitted flags. --n-gpu-layers, CPU ggml, watermark, merging S3/T3/bakers/servers, editing ggml or sibling chatterbox.cpp. Installing packages in case. Inventing Q4_K_M or imatrix. Repeating the cf3efa9 pre-build pause. Treating English v3 as the whole multilingual product. Deleting tokenizer language tokens because C++ does not parse the string today.
