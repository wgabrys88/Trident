standing-order: remaining SST tokenizer facts, GPT-2 pad token, SSoT close, product proofs

Trident standing order — implement this file. Do not invent a second plan.

Work in C:\Users\eb-wjt\Downloads\3-way\Trident on this Windows machine. PowerShell only. No bash, python3, or &&. Use python or .\.venv\Scripts\python.exe. Branch reduction; origin https://github.com/wgabrys88/Trident.git. Preserve untracked desktop.ini and Trident.code-workspace. Do not spawn agents unless asked. Edit live files. Build here. Do not recap instead of working.

Read first, in this order, before any edit: git log -3 --format=%B (this file). Then read every tracked first-party file listed by git ls-files excluding ggml/. The sibling C:\Users\eb-wjt\Downloads\3-way\chatterbox.cpp is spec only. Do not edit it. Do not edit ggml/.

Goal
python tts.py nano|turbo|v3 [flags] TEXT [language] downloads checkpoints, converts GGUF, bakes a voice, reuses or starts one detached named-pipe Vulkan server, writes one 24 kHz WAV, and prints its path. At most one chatterbox-server-*.exe. Same command+voice contract reuses VRAM; any other contract kills the live server then starts the new one. Exclusive server, one flag-default table, and removable JSON quant are the product. v3 is multilingual Llama: the positional language argument is a real user flag, the MTL tokenizer child must receive it, and the GGUF must record the tokenizer language-token inventory.

Execute rules
No tests, pytest, harness, CI, log files, debug prints, golden WAVs, scratch scripts, README, or extra markdown besides this file. No defensive fallbacks. No swallowed errors. No CPU ggml path. No silent f16 when a quant cannot pack. Raise or throw. No narrating comments. Do not pip install. Do not change requirements.txt; keep pykakasi, spacy-pkuseg, dicta-onnx, add-stress-to-epub. Do not add huggingface_hub, scipy, perth, onnxruntime. Torch conversion stays CPU via PYTORCH_CPU_INDEX. CMake: GGML_VULKAN ON, GGML_CPU OFF, GGML_CUDA OFF, GGML_OPENMP OFF. Do not add --n-gpu-layers, --n-threads, watermark, Perth, or a CPU backend. Do not merge GPT-2 and Llama S3, T3, baker, or server binaries. No && in PowerShell; chain with ;.

SST — two owners, never both, never a third
CLI-owned (one table in settings.py: name, default, help, group model|conversion|server, architecture gpt2|llama|both). argparse and host --name pass-through are generated from that table. C++ Flags read those exact names; missing flag throws; zero defaults in C++ or knobs.h. Convert receives --matrix-type/--weight-type and --quant-policy from the host; no convert default when flags are omitted. JSON policy files own rules only. CLI type is the default; never read a JSON "default" key.
CLI names and table defaults:
  seed 42, temperature 0.8, top-k 1000 (gpt2), top-p {gpt2:0.95, llama:1.0}, repeat-penalty 1.2, n-predict 1000, cfm-steps {gpt2:2, llama:5}, trim-fade-samples 480, min-p 0.05 / cfg-weight 0.5 / exaggeration 0.5 / cfm-cfg 0.7 (llama), gpu 0; t3-weight-type q4_0, s3-weight-type q4_0, t3-quant-policy scripts/quant_t3.json, s3-quant-policy scripts/quant_s3.json; variant, TEXT, language (v3 positional required), --reference reference.wav.
A new user flag is one FLAGS row plus generated help, host pass-through, Flags read, Knobs field, engine use. v3 tokenizer filesystem paths stay host-filled, not user flags, not in help.
C++ server flag names must stay hyphen-identical to FLAGS name: --gpu --seed --n-predict --cfm-steps --trim-fade-samples --top-k --temperature --top-p --repeat-penalty --min-p --cfg-weight --exaggeration --cfm-cfg. Llama also consumes --language plus the seven host-filled tokenizer paths.
GGUF-owned (facts of this file, never CLI, never a drifting C++ literal): tensor-derived layout (n_embd, n_head, n_layer, n_ff, embedding sizes, perceiver length, text position length, GPT-2 wpe length); tokenizer facts (GPT-2 token lists/merges, start/stop ids, Llama language-token list); GPT-2 speech pad token and pad count; baked tensors and their lengths; per-tensor ggml dtype. Engine never reads GGUF to decide sampling. No chatterbox.temperature, s3gen.cfm_steps, n_predict, or +4096 n_ctx budget in GGUF. Llama runtime context is GGUF layout plus CLI n-predict plus the graph's fixed special-token slots. GPT-2 context is wpe. Speech vocab/start come from the file or its tensors, not 6561. Do not write chatterbox.n_head for Llama (C++ derives heads from n_embd / (2 * rope_freq_factors.ne[0])). Do not restore unread encoder-geometry kv (attention_heads, ff_size, token_mel_ratio, lookahead, cfm.head_dim, mlp_ratio).
Architecture, not CLI, not a competing GGUF sample-rate key: 15 s GPT-2 / 6 s Llama bake conditioning; Llama drops last 960 samples then Audio::fade; Audio::write is 24 kHz; baker VulkanBackend(0) is the baker device (do not add --gpu to bake.exe; server --gpu stays FLAGS). Do not add those as CLI.

Quant — separate and removable
scripts/quant.py is Policy(default, rules).add only. TYPES from lowercase GGMLQuantizationType names. Integers stay integer. First conjunctive prefix/suffix/contains/ndim match wins, else default. f32/f16/bf16 and packed types go through gguf.quants.quantize; packing errors propagate. No QuantError catch, f16 retry, Q4_K_M recipe, imatrix, or CPU Q4 dequant. Native Vulkan upload retained. F16 3D conv expansion retained. GgufFile::floats requires F32.
Shipped T3 f32: prefixes chatterbox/, voice_encoder/, model/wpe, model/ln_f/, model/norm/, model/rope_freq_factors; suffix /b,/g; ndim 1. Shipped S3 f32: prefixes flow/input_embedding, flow/spk_embed_affine/, s3gen/builtin/, s3gen/mel_fb/, campplus/, s3tokv2/, cfm/, hift/; contains /attn/pos_bias_; suffix /b; ndim 1,3,4. CLI type overrides JSON default; --t3-quant-policy / --s3-quant-policy replace rules. Host conversion contract stores resolved {default, rules}. Names: chatterbox-t3-{variant}-{default}-{sha8}.gguf and chatterbox-s3gen-{family}-{default}-{sha8}.gguf. quant.py plus scripts/quant_t3.json and scripts/quant_s3.json are the whole mechanism.

Exclusive server
One models/server.pid. wanted = {command, voice} with exe, t3, s3, pipe, and every server flag. Migrate leftover models/*.pid. Matching contract and WaitNamedPipeW(pipe, 1000) reuses. Else kill, Popen DETACH, DEVNULL, wait for pipe; process exit or timeout throws. kill is taskkill /F /T /PID, wait, unlink. EngineBuild and VoiceBake kill server.pid before replacing locked files. No host TerminateProcess, siblings, or atexit. Llama tokenizer child TerminateProcess is unrelated. Do not kill the server when tts.py exits.

Architecture already in the graphs
nano/turbo = GPT-2 meanflow, 15 s bake cond. v3 = Llama CFG, 6 s bake cond, multilingual tokenizer. Shared bake_voice in src/common/bake.h; wrappers stay 15 s and 6 s; binaries stay split. HiFT ups/rate/pad and residual groups come from tensor counts (rate=kernel/2, source stride=max(kernel/2,1), pad=(stride-1)/2). CampPlus n_mels and head/xvector counts come from weights. S3 width/mels/speaker/vocab and encoder blocks come from tensors / Weights::count. CFM head from pos_bias_u->ne[0]. Llama speaker2 = 2*mels_. Llama heads from n_embd/(2*rope_freq_factors). Do not merge graphs to save lines.

Already true on reduction at b231c1f — do not redo
Phases A-E are committed: exclusive server, SST table, removable quant, grouped help, shared repeat_penalty/fade, CMAKE_FLAGS/precision_policy/siblings/force_f32 gone. F2 shape/baker work is committed. First-party vs 2adbad0 excluding this file and ggml is 494 insertions / 499 deletions (net -5). That decrease must survive remaining edits except new help strings and the required GGUF-fact writes.
Verified on this clone after user-launched synthesis (20 Sep 2026): 34-45-11-20-09-26_nano.wav, 04-54-11-20-09-26_turbo.wav, 46-43-11-20-09-26_v3.wav exist and human evaluation reported no regression. That does not prove language_tokens, n_ff, pad-token SST, q8_0, layer0, or exclusive-server snapshots on current source after the remaining edits.
Present GGUFs/stamps (user runs, current source has not yet written language_tokens/n_ff/pad keys): chatterbox-t3-{nano,turbo,v3}-q4_0-297be1e6, chatterbox-s3gen-{meanflow,v3}-q4_0-94058b8f, voices/{nano,turbo,v3}/bake.json. Resident server at this rewrite is models/server.pid pid 13192 chatterbox-server-gpt2 turbo default; leave it until the next contract change. models/quant_t3_layer0.json is absent and must be recreated locally (models/ is gitignored). precision1 and 4b9488a1 GGUFs are absent here; do not hunt them. Layer0 hash 182b868d is the expected sha8 when shipped T3 rules get {"prefix":"model/h0/","type":"f32"} prepended.

Code review — required fixes, not a new design
Read every tracked first-party file. Findings that MUST be fixed (these are SST/SSoT/no-fallback bugs, not nits):
1. scripts/convert_t3_llama.py does not write chatterbox.tokenizer.language_tokens or chatterbox.n_ff. Sibling spec convert-t3-v3-to-gguf.py does: language_tokens = sorted(t for t in vocab if re.fullmatch(r"\[[a-z]{2,3}\]", t)); writer.add_string("chatterbox.tokenizer.language_tokens", ",".join(...)); n_ff = int(state["tfmr.layers.0.mlp.gate_proj.weight"].shape[0]); writer.add_uint32("chatterbox.n_ff", n_ff). Language remains CLI. C++ must not start requiring n_ff from kv (FFN already uses tensor shapes). C++ need not parse language_tokens today; still write the string. Do not write chatterbox.n_ctx. Do not write Llama n_head.
2. src/gpt2/t3.cpp generate() still has speech.insert(speech.end(), 3, 4299). Sibling spec is S3GEN_SIL=4299 and SIL_COUNT=3 in include/tts-cpp/chatterbox/gpt2.h. SST: converter writes chatterbox.speech_pad_token and chatterbox.speech_pad_count as uint32; Gpt2T3 reads them with file_.u32 and inserts that many copies. Do not invent a CLI. Do not leave 4299 or a SIL_COUNT literal in C++.
3. scripts/convert_s3.py campplus BN uses ones_like/zeros_like when weight/bias keys are missing. That is a silent fallback. Index the keys directly so a missing tensor raises.
4. host.py wav.unlink(missing_ok=True) is defensive. Keep the re-raise. Unlink only if wav.is_file().
5. EngineBuild contract hashes CMakeLists.txt only, not src. Converter stamps do not hash converter source. After C++ edits delete models/build-contract.json. After T3 converter metadata edits delete the T3 convert.json files that would skip, plus voices/*/bake.json for variants whose T3 changed. After S3 converter behavior edits delete matching S3 convert.json. Leave unused files. Do not add a source-hash walker.
Do not "fix" these (code-judo that would violate the product):
- Do not merge src/gpt2/s3.cpp and src/llama/s3.cpp.
- Do not add --gpu to bake.exe or change bake.h VulkanBackend(0) into a second FLAGS path.
- Do not restore unused encoder kv, chatterbox.n_ctx, Llama n_head, text_frontend, n_batch, competing sample-rate keys, or CLI sampling into GGUF.
- Do not split files; none are over 1000 lines (largest first-party: llama/s3.cpp 384, gpt2/s3.cpp 381, host.py 343).
- Do not add ad-hoc argparse outside settings.FLAGS.
- Do not read JSON "default"; CLI type is the default.
- Do not edit ggml/ or the sibling engine.
- Do not add tests, packages, README, or extra markdown besides this file.

Stamp deletion after the converter/C++ edits (do this, then synthesize)
T3 metadata will change for gpt2 and llama, and Gpt2T3 will require the new pad keys, so old T3 GGUFs cannot be reused:
  Remove-Item models\chatterbox-t3-nano-q4_0-297be1e6.convert.json, models\chatterbox-t3-turbo-q4_0-297be1e6.convert.json, models\chatterbox-t3-v3-q4_0-297be1e6.convert.json
  Remove-Item models\voices\nano\bake.json, models\voices\turbo\bake.json, models\voices\v3\bake.json
  Remove-Item models\build-contract.json
If convert_s3.py only lost the BN fallback and checkpoints contain the keys, leave S3 convert.json in place. Recreate models\quant_t3_layer0.json (gitignored) as:
{"rules":[{"prefix":"model/h0/","type":"f32"},{"prefix":"chatterbox/","type":"f32"},{"prefix":"voice_encoder/","type":"f32"},{"prefix":"model/wpe","type":"f32"},{"prefix":"model/ln_f/","type":"f32"},{"prefix":"model/norm/","type":"f32"},{"prefix":"model/rope_freq_factors","type":"f32"},{"suffix":"/b","type":"f32"},{"suffix":"/g","type":"f32"},{"ndim":1,"type":"f32"}]}
Do not git add anything under models/.

Remaining work — do this next, in order, without a new design phase
- [ ] LlamaConverter.convert in scripts/convert_t3_llama.py: from the already-loaded Tokenizer vocab write chatterbox.tokenizer.language_tokens as a comma-joined string of tokens matching \[[a-z]{2,3}\] (sorted); add n_ff=int(state["tfmr.layers.0.mlp.gate_proj.weight"].shape[0]) to the chatterbox uint32 metadata dict. Language remains CLI. add_string is not T3Converter.metadata(); call writer.add_string directly. Do not add a tokenizer token list for Llama (runtime encoder is the checkpoint JSON).
- [ ] Gpt2Converter.metadata integers: speech_pad_token=4299, speech_pad_count=3. Gpt2T3 stores those two u32 fields and generate() inserts pad_count copies of pad_token. Delete the 4299 literal. Confirm with a search that 4299 and 6561 are gone from first-party C++/headers.
- [ ] convert_s3.py: drop ones_like/zeros_like BN fallbacks. host.py: unlink wav only if it exists; still re-raise.
- [ ] Delete the stamps listed above. Recreate models/quant_t3_layer0.json locally. Next tts.py run rebuilds because build-contract.json is gone.
- [ ] python tts.py nano -h and python tts.py v3 -h show Model / GGUF conversion / Server from the one table. Defaults match the table including --gpu 0. v3 help includes language. Tokenizer paths absent from help. No convert/C++ defaults appear as a second number.
- [ ] Prove current binaries+GGUFs after the edits: v3 English (positional en); v3 with a second language that exercises the MTL child (ja, zh, he, or ru). Then default nano, turbo, v3 so a snapshot after nano then turbo then v3 shows exactly one chatterbox-server-*.exe (Get-Process chatterbox-server*). Same flags reuse the pid. Flag or model change unloads first. Each success prints one WAV path; file is 24 kHz mono. Record actual paths, rates, durations, pids in the next rewrite of this file. User WAVs already in the tree do not count for current source after these edits.
- [ ] python tts.py nano --t3-weight-type q8_0 TEXT reconverts, rebakes, restarts (new t3 filename contains q8_0, new pid). python tts.py nano --t3-quant-policy models/quant_t3_layer0.json TEXT pins layer 0 with no code change (expected T3 sha8 182b868d).
- [ ] SSoT close, grep the live tree (not ggml, not sibling): settings.FLAGS is the only CLI default table; C++ Flags throws on missing/unknown; knobs.h has no default member values; convert_t3.py / convert_s3.py still require --matrix-type/--weight-type and --quant-policy; host pass-through is generated from FLAGS server rows; no JSON "default" is read; no second argparse; no host TerminateProcess; no force_f32, siblings, CMAKE_FLAGS, precision_policy.json; no chatterbox.temperature / s3gen.cfm_steps / n_predict written to GGUF. If any path overrides a FLAGS value with a different literal, delete that override. Do not add architecture constants as CLI to fake completeness.
- [ ] git diff --stat 2adbad0 HEAD excluding this file and ggml remains a net first-party decrease except allowed new help and the new GGUF-fact writes. Restore language_tokens/n_ff/pad by adding facts, not by duplicating bake/S3 glue.
- [ ] Static close: requirements.txt unchanged; ggml and sibling untouched; no tests, extra pip, CPU backend, watermark, or merged graphs; quant.py + two shipped JSONs remain the whole quant mechanism.
- [ ] Only then rewrite this entire file as a maintenance standing order with the same mission/SST/quant/server/prohibitions/ritual and an empty work checklist. Commit -F. Stop. Never mark the goal complete because nano/turbo/v3 succeeded before language_tokens, n_ff, pad-token SST, q8_0, or layer0 are verified on current source.

Exact implementation notes so a weaker agent cannot omit them
LlamaConverter already has vocab = Tokenizer.from_file(...).get_vocab() and a metadata(...) call. Extend that integers dict with n_ff. After tokenizer vocab is in hand, before or after metadata:
  import re is already there.
  self.writer.add_string("chatterbox.tokenizer.language_tokens", ",".join(sorted(t for t in vocab if re.fullmatch(r"\[[a-z]{2,3}\]", t))))
Gpt2Converter.metadata integers already include n_embd, n_head, n_layer, speech_vocab_size, start_speech_token, stop_speech_token. Add speech_pad_token and speech_pad_count there. Gpt2T3 constructor already reads start_/stop_/vocabulary_ via file_.u32; add pad_token_ and pad_count_ the same way. generate() currently: speech.insert(speech.end(), 3, 4299); replace with speech.insert(speech.end(), pad_count_, pad_token_);
Help already comes from Host.parser groups Model / GGUF conversion / Server. Do not reimplement help. Run it and record that it matches FLAGS.
v3 command shape: python tts.py v3 "Hello from Trident." en
v3 Japanese (or zh/he/ru): python tts.py v3 "こんにちは。" ja
Reuse: second identical command keeps the same pid. Get-Process chatterbox-server* must show one process after every successful snapshot.
q8_0 and layer0 are nano-only proofs; they must reconvert T3, rebake nano, restart the exclusive server.

Commit ritual
After every phase boundary: read this file in full; rewrite the entire file from zero: first line is the git subject, blank line, then the standing order with actual completed vs remaining work and the real pid/wav evidence. Stage implementation and this file; git commit -F PROMPT-BOOTSTRAP-POSTERITY.md. Push origin/reduction. Continue the remaining list immediately unless the user asked to pause. No dummy subject, no append-only changelog, no empty-checklist maintenance status while work remains. This prompt-only commit is not a pause.

Do not
Plan-only replies, permission questions, sandbox trees, or restating this file instead of editing. Tests, harnesses, loggers, extra markdown. Fallbacks, C++ knob defaults, convert weight-type defaults when the host omitted flags. --n-gpu-layers, CPU ggml, watermark, merging S3/T3/bakers/servers, editing ggml or sibling chatterbox.cpp. Installing packages in case. Inventing Q4_K_M or imatrix. Repeating the cf3efa9 pre-build pause. Treating English v3 as the whole multilingual product. Deleting tokenizer language tokens because C++ does not parse the string today. Treating the user's pre-edit WAV files as proof of the post-edit source. Hashing every source file into contracts instead of deleting the stamps listed above.
