phase-E checkpoint before local validation

Trident standing order and compaction handoff

Work on the real Windows machine in C:\Users\eb-wjt\Downloads\3-way\Trident. Shell is PowerShell: no bash, python3, or &&. Use python or .\.venv\Scripts\python.exe. Branch is reduction; origin is https://github.com/wgabrys88/Trident.git. Preserve unrelated untracked desktop.ini.

CURRENT USER OVERRIDE: Stop work after committing this full file as the complete commit message and pushing. The user requested a pause to compact the session. Resume only when the user asks to resume. On resume, read git log -1 --format=%B and this entire file, then continue Phase F and any corrections without asking for permission. Do not treat this checkpoint as completion.

Product mission
python tts.py nano|turbo|v3 [flags] TEXT [language] downloads checkpoints, converts GGUF, bakes a reference voice, reuses or starts one detached named-pipe Vulkan server, writes one WAV, prints its path. At most one chatterbox-server-*.exe may remain resident. Same command+voice contract reuses VRAM; a different contract unloads the old process before starting the new one. Keep GPT-2 nano/turbo meanflow and Llama v3 CFG libraries, T3, S3, bakers and server executables separate. GPT-2 bake conditioning is 15 seconds, Llama 6 seconds, Llama audio drops its final 960 samples before the shared fade. Product output is 24 kHz, not a user flag or competing GGUF output-rate key.

Implemented and committed, still awaiting local build/runtime validation
- [x] A, 45a5636: models/server.pid replaces per-variant PID files. PipeServer.ensure migrates every other models/*.pid with kill(), compares command+voice and probes the matching pipe for 1000 ms before reuse. Mismatch kills, starts with existing DETACH flags and DEVNULL, writes PID contract, waits for the pipe with timeout/process-exit errors. kill uses taskkill /F /T /PID then waits for process exit and unlinks, with no duplicate TerminateProcess. EngineBuild and VoiceBake kill server.pid before replacing locked files. ARCHITECTURES siblings and kill loop removed. Initial leaked servers were stopped, then legacy PID files were removed through the new host.kill code. No atexit cleanup.
- [x] B, c8bff24: settings.FLAGS is the sole flag/default/help/group/architecture table. Host builds argparse and exact server flag names from it. C++ Flags requires every value; Knobs has no defaults. gpu=0 is passed to both servers and VulkanBackend(device); initialization failure throws. CMakeLists.txt hash, ggml revision, generator and arch form build contract; copied CMAKE_FLAGS removed. Llama context uses GGUF perceiver/text layout plus CLI n-predict and fixed graph slots; converters no longer write n_ctx. GPT-2 context remains wpe tensor length. Speech caps read GGUF; converters derive speech token boundary from S3 checkpoint input_embedding rows and speech/text vocabulary dimensions from checkpoint tensors.
- [x] C, d93f30c: scripts/quant.py is architecture-independent Policy(default,rules), TYPES comes from gguf.GGMLQuantizationType. Integer input stays integer. First matching prefix/suffix/contains/ndim rule selects type; all supplied match fields must match. Otherwise default. gguf.quants.quantize handles f32/f16/bf16/quant packing without catches, retries or block-size special cases. Writer uses raw_dtype for packed shape handling. T3Converter is in scripts/convert_t3.py, old quant_policy.py and precision_policy.json removed. Converters use policy.add; force_f32 removed. Required converter type and policy arguments are supplied by host. Resolved default+rules is in conversion contracts; GGUF names include default and canonical rules sha256[:8]. GgufFile::floats rejects anything but F32. Native Vulkan upload and F16 3D convolution expansion preserved.
- [x] D, ed6bfcb: root help selects nano/turbo/v3; architecture help has Model, GGUF conversion, Server groups plus argparse's standard help option. Table help describes defaults, rule mixing, integer preservation, rebuild/rebake/restart, Q4_K_M not being a type, and server restarts with GGUF reuse. Types are derived dynamically from TYPES, not a copied enum list. Tokenizer paths remain private host-filled arguments.
- [x] E, 663337f: repeat penalty is shared in src/common/repeat_penalty.h; both engines call Audio::fade; Llama keeps its 960-sample tail removal. Narrating BatchNorm comment removed. Quant JSON formatting compacted. Net first-party line reduction still needs a final measured audit rather than assumption.

Actual machine state at pause
The local build was started only AFTER the phase-E full-message commit, by python tts.py nano 'Hello from Trident.'. CMake configured successfully with MSVC 19.44, Windows SDK 10.0.26100.0, Vulkan SDK 1.4.357.0, Vulkan enabled and CPU/CUDA/OpenMP disabled. The build was compiling Vulkan shaders when the user requested pause. The whole Python launcher/build process tree was stopped using taskkill /F /T /PID 2148. No completed build or generated WAV is claimed. Partial build files remain and can be resumed through the normal host invocation. The initial executor session was 31150, but do not depend on that session after compaction. There were no known compilation errors before interruption. CMake emitted normal assembler-policy/unused-variable/install-rule warnings. Do not install packages to address them.
The installed .venv requirements stamp matches requirements.txt and the CPU torch bootstrap settings. Existing ggml HEAD is the pinned 7840aaba1989c6deeefede1d77d5aaf8f52b947e. No package installation or ggml source modification was performed. python tts.py -h, nano -h and v3 -h were exercised successfully. No automated tests or harnesses were written or run. The standing-order file did not exist initially and was created for phase A.

Remaining Phase F and review corrections
- [ ] Resume the local build and actual synthesis with python tts.py nano 'Hello from Trident.'. Fix real build/conversion/bake/runtime failures without fallbacks or extra installations. Build all four separate executables locally. Do not mark completion until this succeeds.
- [ ] Run nano then turbo then v3 and inspect live processes/PID contracts: exactly one chatterbox-server-*.exe. Same flags reuse its PID. Changed flag/model kills old first and starts the requested contract. Do not kill the final server merely on tts.py exit.
- [ ] Exercise --t3-weight-type q8_0, confirm reconversion/rebake/restart and native stored types. Exercise a JSON rule {"prefix":"model/h0/","type":"f32"} prepended to shipped rules and confirm layer zero is F32 with other matrix layers retaining chosen default. Use real conversion/inference, not a test harness, scratch script, or golden WAV.
- [ ] Audit CLI/GGUF ownership, including lingering shape/embedding literals in existing bakers and converter metadata. Shape facts and voice lengths must derive from tensors/metadata, never driftable C++ literals. Current bakers still visibly use speaker replacement shape {256,1} and feature width 80; review these against the strict ownership requirement. Existing architecture-specific attention geometry also warrants review. Avoid expanding scope into merging engines.
- [ ] Confirm quant files are the entire quant mechanism and all converters call policy.add. Shipped JSON contains rules, while q4_0 default lives only in settings; CLI default overrides any JSON default. Unsupported gguf packing types must fail explicitly, not silently choose f16. Keep required F32 rules, especially arrays consumed by GgufFile::floats.
- [ ] Review final help quality and small cleanup: settings weight-type help currently has a harmless redundant + '' expression. No duplicate help stub/default dictionaries. Root parser and model table variant ownership should remain coherent.
- [ ] Measure net first-party lines versus parent of phase A, excluding standing-order text and allowing added help strings; reduce duplicate glue if needed. No cosmetic compression that obscures code. Requirements must be unchanged, ggml and sibling repo untouched. Audit no force_f32, siblings, legacy precision JSON or host TerminateProcess. Llama tokenizer's existing child-process TerminateProcess is not the removed host dual-kill mechanism.
- [ ] Only after all requirements are satisfied, rewrite this file entirely as a maintenance standing order with an empty work checklist, preserving the invariants and commit ritual, then commit the full file as the complete git message. Push if still authorized by the resumed user's instruction; this checkpoint push is explicitly authorized now.

Permanent SST rules
CLI owns seed, temperature, GPT-2 top-k, top-p, repeat-penalty, n-predict, cfm-steps, trim-fade-samples, Llama min-p/cfg-weight/exaggeration/cfm-cfg, gpu; conversion type and policy flags; model variant/TEXT/language/reference. Defaults exist in settings.FLAGS only. GPT-2 top-p=.95 and cfm-steps=2; Llama top-p=1 and cfm-steps=5; all original numeric defaults otherwise preserved. C++ requires exact --names with no defaults. New flag means one settings row, generated help/host pass-through, C++ Flags read, Knobs field, engine use.
GGUF owns shapes, head/layer/feed-forward/embedding sizes, perceiver/text/wpe positions, tokenizer tokens/IDs/languages, baked tensors and lengths, and actual per-tensor stored dtype. No sampling authority in GGUF. Llama runtime budget is CLI n-predict plus file layout, never a converted +4096 budget. CMakeLists.txt is build-option truth: Vulkan ON; CPU/CUDA/OpenMP OFF.
Quant rules: first match wins; prefix/suffix/contains/ndim fields conjunctive; integer arrays never quantized. T3 F32 prefixes chatterbox/, voice_encoder/, model/wpe, model/ln_f/, model/norm/, model/rope_freq_factors; suffix /b and /g; ndim 1. S3 F32 prefixes flow/input_embedding, flow/spk_embed_affine/, s3gen/builtin/, s3gen/mel_fb/, campplus/, s3tokv2/, cfm/, hift/; suffix /b; ndim 1,3,4. Layer matrix weights otherwise fall through to selected default. No llama.cpp Q4_K_M recipes or imatrix. Keep scripts/quant.py plus quant_t3.json and quant_s3.json removable independently of the engine.

Prohibitions
No tests, pytest, harnesses, CI, log files, debug prints, golden WAVs, scratch scripts, README or extra markdown. No defensive fallback code, swallowed errors, CPU fallback, or f16 retry. No pip installs or requirements changes; retain pykakasi, spacy-pkuseg, dicta-onnx and add-stress-to-epub because v3 imports them. Do not add huggingface_hub, scipy, perth or onnxruntime to requirements. Torch conversion remains CPU. Do not edit ggml/ or C:\Users\eb-wjt\Downloads\3-way\chatterbox.cpp. No watermark, Perth, --n-gpu-layers, --n-threads, CPU ggml backend, or merging GPT-2/Llama binaries/graphs/bakers. No permission requests for already authorized work. Respect the current explicit pause.

Commit ritual
After every phase, including F: read this file in full, replace it ENTIRELY from scratch with the mission, actual completed/remaining work and discovered facts. First line is the git subject; blank line; full updated standing order follows. Stage implementation and this file; git commit -F PROMPT-BOOTSTRAP-POSTERITY.md. No short dummy body, appended changelog, or unsupported completion claim. Resume the next authorized phase immediately unless user has requested a pause, as they have at this checkpoint.
