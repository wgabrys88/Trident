phase-F checkpoint after F2 shape audit nano turbo

Trident standing order and compaction handoff

Work on the real Windows machine in C:\Users\eb-wjt\Downloads\3-way\Trident, PowerShell only. No bash, python3, or &&. Use python or .\.venv\Scripts\python.exe. Branch reduction; origin https://github.com/wgabrys88/Trident.git. Preserve unrelated untracked desktop.ini and Trident.code-workspace. Do not spawn agents unless asked.

Mission
python tts.py nano|turbo|v3 [flags] TEXT [language] downloads checkpoints, converts GGUF, bakes a voice, reuses or starts one detached named-pipe Vulkan server, writes one WAV, and prints its path. At most one chatterbox-server-*.exe on the machine. Same command+voice contract reuses VRAM; a changed contract kills the previous server before starting another. Keep GPT-2 nano/turbo meanflow and Llama v3 CFG as separate libraries, S3/T3 graphs, bakers and server binaries. GPT-2 bake conditioning is 15 seconds; Llama is 6 seconds. Llama removes its final 960 samples before shared Audio::fade. Product output is 24 kHz, not a user flag or competing GGUF output-rate key.

Completed implementation
- [x] Phase A, 45a5636: one models/server.pid. PipeServer.ensure migrates other models/*.pid via kill, compares command+voice, probes matching pipe for 1000 ms, reuses or kills then Popen with DETACH and DEVNULL. Startup exit/timeout throws. kill uses taskkill /F /T /PID, waits, unlinks. No host TerminateProcess, siblings, or atexit. Build/bake kill server.pid before replacing locked files.
- [x] Phase B, c8bff24: settings.FLAGS owns names/defaults/help/groups/architecture. Generated argparse and exact host pass-through. C++ mandatory flags, no defaults. gpu=0 reaches VulkanBackend(device); failure throws. Build contract hashes CMakeLists.txt plus ggml revision/generator/architecture; CMAKE_FLAGS removed. Llama context uses GGUF layout plus n-predict; no converted n_ctx/+4096. GPT-2 context is wpe length. Speech caps come from GGUF.
- [x] Phase C, d93f30c: quant.py Policy(default,rules), TYPES from GGMLQuantizationType; integer tensors remain integer, first matching conjunctive rule wins, packing errors propagate. No quant fallbacks. T3Converter moved into convert_t3.py; quant_policy.py and precision_policy.json deleted. Converters require type/policy args and use policy.add. Host conversion/bake contracts store resolved default+rules. Names contain default plus canonical rules SHA256[:8]. GgufFile::floats requires F32. Native Vulkan weights and F16 3D convolution expansion retained.
- [x] Phase D, ed6bfcb: root/variant help from table, Model/GGUF conversion/Server groups, enum types, integer preservation, JSON mixing, restart/reconvert explanation. Tokenizer paths private host-filled arguments.
- [x] Phase E, 663337f: shared repeat_penalty.h and Audio::fade, Llama tail retained; BatchNorm narrating comment removed. Checkpoints e9c3fb3 and f73d1f6 preserve earlier build work.
- [x] f73d1f6 shape corrections and cf3efa9 pos_bias F32 / Weights::count / HiFT kernel / kaldi-channel C++.
- [x] F1, f943e95: rebuilt four Vulkan binaries and proved that committed C++ with nano reuse/replace, turbo, and v3. Historical PIDs 3064 and 9300 remain unproven.

F2 source on disk in this commit
Shared baker glue is src/common/bake.h bake_voice(t3,s3,reference,condition_seconds) with thin src/gpt2/baker.cpp (15 s) and src/llama/baker.cpp (6 s) wrappers. Bakers no longer write unread prompt_token_len / prompt_feat_frames / cond_prompt_length; cond_prompt_max remains the tokenize cap. Baker and server binaries stay separate. bake.h is listed on trident-common in CMakeLists.txt so the cmake-hash build contract rebuilds after this glue; do not merge S3/T3 graphs.
HiFT in both S3 files: upsample count from Weights::count("hift/ups/","/weight"), residual groups = resblock count / ups, rate = kernel/2, source stride = max(kernel/2,1), pad = (stride-1)/2, last-stage extra sample when i+1==ups. Deleted rates[] / source_strides[] / source_padding[] and the literal for (i<3).
CampPlus n_mels from flow.encoder_proj.weight out dim; feat_dim from campplus/mel_fb_kaldi_80->ne[1]; head layers/blocks and xvector block layers from tensor counts; kernel from linear_local ne[0]. Converter no longer uses literal 80 / layer<=2 / block<2.
S3 width/mels/speaker/vocab from flow/input_embedding and spk_embed_affine tensors; encoder/up blocks from Weights::count; CFM head from flow/encoder/block0/attn/pos_bias_u->ne[0]. Llama speaker2 is 2*mels_. GPT-2 T3 context from wpe; conditioning length from cond_prompt_speech_tokens; Llama heads from n_embd/(2*rope_freq_factors.ne[0]). Tokenizer head_dim_ = n_audio_state/n_audio_head.
Stripped unused/competing GGUF kv: s3gen input/output/spk/encoder geometry/cfm.head_dim/layer_norm_eps/builtin prompt lengths; tokenizer head_dim/mlp_ratio/codebook_size/sample_rate; llama n_head/n_ff/n_batch/text_vocab/speaker_embed/text_frontend/language_tokens; VE n_fft/hop/win/overlap; campplus feat_dim/embedding_size/sample_rate/kernel_size/blockN_layers; cond_prompt_length. Kept FFT/hop/stride, special tokens, rope_theta/orig_ctx, fsq_levels, seg_pool_len, dilations, GPT-2 n_head, perceiver_heads. No CLI sampling authority in GGUF.

F2 rebuild and synthesis verified on this machine
Deleted only active convert.json hashes 297be1e6 / 94058b8f / 182b868d and voices/{nano,turbo,v3}/bake.json. Left precision1, 4b9488a1, unused files, and models/quant_t3_layer0.json. python tts.py nano rebuilt because CMakeLists.txt hash changed. MSVC 19.44.35228.0, Windows SDK 10.0.26100.0, Vulkan SDK 1.4.357.0. Vulkan ON; CPU/CUDA/OpenMP OFF. ggml HEAD 7840aaba1989c6deeefede1d77d5aaf8f52b947e. Four binaries under build/bin timestamps 10:19 20 Sep 2026. Reconverted nano T3 297be1e6 and meanflow S3 94058b8f, rebaked nano, then turbo T3 and turbo bake. v3 94058b8f/297be1e6 convert stamps and v3 bake.json are still absent so the next v3 run must reconvert and rebake. Layer0 182b868d convert.json is still absent.
- [x] Nano default after F2 rebuild: 39-14-10-20-09-26_nano.wav, 24 kHz, 2.04 s, PID9836 chatterbox-server-gpt2. Exactly one server.
- [x] Same nano contract reused PID9836: 10-20-10-20-09-26_nano.wav, 24 kHz, 2.04 s. Exactly one server. Runtime about 4.1 s, no rebuild.
- [x] Turbo default replaced nano with PID13204 chatterbox-server-gpt2: 18-20-10-20-09-26_turbo.wav, 24 kHz, 1.84 s. Exactly one server.
- [x] Working-tree first-party vs 2adbad0 excluding this prompt and ggml: 448 insertions, 499 deletions, plus untracked bake.h 46 lines, net -5. Requirement is net decrease apart from help; this cut is genuine baker-duplicate removal and unused-kv deletion, not cosmetic compression. After this commit, git diff --stat 2adbad0 HEAD excluding the prompt must still show a decrease.
- [x] Final resident server at this pause is PID13204 turbo default; leave it until the next contract change (v3 or layer0). Do not invent a cause for historical two-PID snapshots.

This is NOT Phase F completion. F2 C++/converter/baker source is in this commit and nano/turbo ran on those binaries. v3, layer0 JSON nano, and the maintenance handover are still open. Never mark Phase F complete because nano/turbo succeeded before v3/layer0/F3.

Remaining Phase F
- [ ] Real v3 English synthesis after F2 GGUF metadata with one exclusive server (must reconvert v3 T3/S3 and rebake). Keep the final server resident.
- [ ] Re-run layer0 JSON nano: python tts.py nano --t3-quant-policy models/quant_t3_layer0.json TEXT. T3 converter kv changed; 182b868d convert.json was deleted and has not been rewritten.
- [ ] After v3/layer0, confirm git diff --stat 2adbad0 HEAD excluding this prompt and ggml remains a net first-party decrease.
- [ ] Final static audit: requirements unchanged, ggml and sibling untouched; no force_f32, siblings, CMAKE_FLAGS, precision_policy.json or host TerminateProcess; existing Llama tokenizer TerminateProcess is unrelated. No tests, pip installs, CPU backend, watermark or merged graphs. quant.py and two shipped JSONs remain the entire quant mechanism.
- [ ] Only when every item is complete, rewrite this entire file as maintenance standing order with empty work checklist, commit full-message and finish.

Permanent SST ownership
CLI owns seed, temperature, GPT-2 top-k, top-p, repeat-penalty, n-predict, cfm-steps, trim-fade-samples, Llama min-p/cfg-weight/exaggeration/cfm-cfg, gpu; t3/s3 weight type and policy; variant/TEXT/language/reference. Defaults exclusively settings.FLAGS: seed42, temperature.8, top-k1000, repeat1.2, n-predict1000, trim480, gpu0; GPT-2 top-p.95/cfm2; Llama top-p1/cfm5/min-p.05/cfg.5/exaggeration.5/cfm-cfg.7; q4_0 defaults, shipped JSONs, reference.wav. Host exact --names, C++ requires all without defaults. A new flag requires one table row, generated help/pass-through, Flags read, Knobs field, engine use. CMakeLists.txt alone owns compile switches.
GGUF owns tensor shapes, head/layer/FF/embedding sizes, perceiver/text/wpe layout, tokenizer tokens/IDs/languages, baked tensor lengths, and stored per-tensor types. No CLI sampling authority in GGUF, no +4096 Llama context budget, no ignored competing output sample rate.

Quant contract
Policy(default,rules). Integer arrays remain integer. First conjunctive prefix/suffix/contains/ndim match wins, else default. TYPES are lowercase GGMLQuantizationType names. f32/f16/bf16 and packed types go through gguf.quants.quantize with errors propagated. No Q4_K_M recipe, imatrix, quant catch/retry/f16 fallback, or CPU Q4 dequant. Native Vulkan upload retained; F16 3D conv expansion retained. GgufFile::floats rejects nonF32.
T3 F32: prefixes chatterbox/, voice_encoder/, model/wpe, model/ln_f/, model/norm/, model/rope_freq_factors; suffix /b,/g; ndim1. S3 F32: prefixes flow/input_embedding, flow/spk_embed_affine/, s3gen/builtin/, s3gen/mel_fb/, campplus/, s3tokv2/, cfm/, hift/; contains /attn/pos_bias_; suffix /b; ndim1,3,4. Other matrices default q4_0. JSONs own rules, settings owns default. Changing rules or default reconverts/rebakes/restarts. Filenames are chatterbox-t3-{variant}-{default}-{sha8}.gguf and chatterbox-s3gen-{family}-{default}-{sha8}.gguf where sha8 hashes canonical rules JSON.

Prohibitions
No tests, pytest, harnesses, CI, log files, debug prints, golden WAVs, scratch scripts, README, or extra markdown. No defensive fallbacks or swallowed errors. No pip installs or requirements edits; language packages pykakasi, spacy-pkuseg, dicta-onnx, add-stress-to-epub stay. Do not add huggingface_hub/scipy/perth/onnxruntime. Torch conversion stays CPU via PYTORCH_CPU_INDEX. Do not edit ggml/ or C:\Users\eb-wjt\Downloads\3-way\chatterbox.cpp. No watermark/Perth, --n-gpu-layers, --n-threads, CPU ggml backend, or merged GPT-2/Llama engines/bakers/servers. No unauthorized messages to others or unnecessary permission questions.

Commit ritual
After every phase including F: read this file in full; rewrite ENTIRE file from zero with mission, actual completed/remaining work and discoveries. First line is git subject, blank line, full standing order. Stage implementation and this file; git commit -F PROMPT-BOOTSTRAP-POSTERITY.md. No short dummy message, append-only changelog, or premature maintenance status. Continue next phase unless the user requested pause. Push origin/reduction after each phase commit. The user requested this compaction pause after the F2 nano/turbo checkpoint. Resume remaining v3, layer0 nano, static audit, and empty-checklist maintenance handover immediately after reading the committed message. Do not repeat the already-satisfied pre-build pause from cf3efa9.
