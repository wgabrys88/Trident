phase-F checkpoint after build and first runtime failure

Trident standing order and compaction handoff

Work on the real Windows machine in C:\Users\eb-wjt\Downloads\3-way\Trident. PowerShell only: no bash, python3, or &&. Use python or .\.venv\Scripts\python.exe. Branch reduction; origin https://github.com/wgabrys88/Trident.git. Preserve unrelated untracked desktop.ini. Do not spawn agents unless asked.

CURRENT USER OVERRIDE: Commit current implementation with this entire rewritten file as the complete commit message, push, and PAUSE for compaction. Resume only upon the user's request. On resume read git log -1 --format=%B and this entire file, then continue all remaining work without asking permission. This checkpoint is NOT Phase F completion.

Mission
python tts.py nano|turbo|v3 [flags] TEXT [language] downloads checkpoints, converts GGUF, bakes a voice, reuses or starts one detached named-pipe Vulkan server, writes one WAV, and prints its path. At most one chatterbox-server-*.exe on the machine. Same command+voice contract reuses VRAM; a changed contract kills the previous server before starting another. Keep GPT-2 nano/turbo meanflow and Llama v3 CFG separate libraries, S3/T3 graphs, bakers and server binaries. GPT-2 bake conditioning is 15 seconds; Llama is 6 seconds. Llama removes its final 960 samples before shared Audio::fade. Product output is 24 kHz, not a user flag or competing GGUF output-rate key.

Completed implementation phases
- [x] A (45a5636): models/server.pid replaces per-variant PID scheme. PipeServer.ensure migrates every other models/*.pid via kill, compares command+voice and probes matching pipe for 1000 ms to reuse. Otherwise kills then starts with DETACH flags and DEVNULL, writes PID contract, waits for pipe with exit/timeout errors. kill uses taskkill /F /T /PID, waits for exit and unlinks; no host TerminateProcess. Build and bake kill server.pid before replacing locked files. ARCHITECTURES siblings and kill loop removed. Initial leaked servers were stopped; old PID files were migrated through new code. No atexit.
- [x] B (c8bff24): settings.FLAGS owns flag names/defaults/help/groups/architecture. Argparse and exact host server flags generated from rows. C++ requires flags with no defaults. gpu=0 passes through both servers to VulkanBackend(device), throws on failure. Build contract hashes CMakeLists.txt plus ggml revision/generator/arch; CMAKE_FLAGS removed. Llama context is file perceiver/text layout plus CLI n-predict and fixed special slots, no converted n_ctx/+4096. GPT-2 context comes from wpe. Speech caps read GGUF; token boundaries derived from S3 input embedding rows.
- [x] C (d93f30c): quant.py contains generic Policy(default,rules), TYPES from GGMLQuantizationType. Integer arrays stay integer. First conjunctive prefix/suffix/contains/ndim match wins, otherwise default. gguf.quants.quantize packs f32/f16/bf16/quant types with no retries or caught packing errors, writer raw_dtype preserves packed shapes. T3Converter moved out of removed quant_policy.py into convert_t3.py. precision_policy.json deleted. All converter tensors use policy.add; force_f32 gone. Converter type/policy args required. Host contracts store resolved default+rules. GGUF filenames contain default plus canonical rules SHA256[:8]. GgufFile::floats rejects non-F32. Native Vulkan upload and F16 3D convolution expansion retained.
- [x] D (ed6bfcb): generated root/variant help; Model, GGUF conversion, Server groups plus standard argparse help. Defaults and architecture-specific flags come from table; types from enum. Help explains rule mixing, unchanged integers, conversion rebuild/rebake/restart, Q4_K_M not a type, and server restart with matching GGUF reuse. Tokenizer paths remain host-filled private flags.
- [x] E (663337f): common repeat_penalty.h replaces two copies, common Audio::fade replaces engine duplicates; Llama tail removal retained. BatchNorm narrating comment removed. Prior compaction checkpoint e9c3fb3 was pushed before this session.

Corrections included in this checkpoint
- Both bakers derive speaker tensor length from speaker.size() and mel width from the GGUF mel filter shape.
- T3 voice encoder metadata derives mel/hidden/layer/embedding dimensions from ve.safetensors. Actual projection key is proj.weight, not linear.weight. Builtin speaker reshape uses -1.
- S3 metadata derives input/output/speaker dimensions, conformer block counts/head geometry/feed-forward size, tokenizer mel/state/layer/MLP/FSMN/FSQ dimensions and codebook size from checkpoint tensors. Tokenizer mel filters use checkpoint tokenizer._mel_filters. CampPlus embedding dimension and dense-block counts derive from tensors.
- Both separate S3 graphs now use file dimensions for mel/input/speaker sizes, conformer geometry and counts, CFM projection width, residual channel size, time embedding sizes, and HiFT upsample kernels/channels. New s3gen.cfm.head_dim metadata is 64 in converter; C++ reads it. Llama perceiver head count is now chatterbox.perceiver_heads metadata (4 in converter), not a second C++ literal. Some architectural head geometry cannot be inferred from flattened projection shape alone; further ownership review remains.
- Shared T3Converter.main generates required converter CLI for separate GPT-2 and Llama subclasses, deleting duplicate argparse code. Removed redundant empty help concatenation and unused host REF constant.
- Fixed actual MSVC build error by including <string> in vulkan_backend.cpp for std::to_string(device).

Actual local build and runtime evidence
- [x] All FOUR executables built successfully locally: chatterbox-server-gpt2.exe, chatterbox-bake-gpt2.exe, chatterbox-server-llama.exe, chatterbox-bake-llama.exe. Separate trident-gpt2/trident-llama libraries linked successfully. CMake uses MSVC 19.44, Windows SDK 10.0.26100.0, Vulkan SDK 1.4.357.0; Vulkan ON and CPU/CUDA/OpenMP OFF. Build warnings in untouched ggml are not errors. No package installation or ggml edits performed.
- [x] python tts.py nano -h and v3 -h ran successfully again, required groups/defaults and --gpu 0 present. Earlier root help also succeeded. git diff --check passed (only CRLF normalization warnings).
- [x] Nano default conversion and voice bake completed through python tts.py nano 'Hello from Trident.'. Files: models/chatterbox-t3-nano-q4_0-297be1e6.gguf (276532032 bytes), models/chatterbox-s3gen-meanflow-q4_0-4b9488a1.gguf (945755712 bytes), their .convert.json contracts, models/voices/nano/t3.gguf and s3.gguf plus bake.json. Old precision1 files remain untouched.
- [ ] FIRST REAL SYNTHESIS FAILED. PipeServer.synthesize received an empty/nonconforming acknowledgement and failed at status, body = ack.split(" ", 1): ValueError expected 2, got 1. No successful WAV is claimed. Host removes incomplete output on failure and rethrows. Detached server stderr is DEVNULL, so its actual failure is not yet known.
- A process snapshot during this invocation reported TWO chatterbox-server-gpt2 processes: PID 3064 (CPU .83) and PID 9300 (CPU unavailable); python PID 7780. models/server.pid records PID 3064 with nano command/voice contract. A subsequent Win32_Process enumeration and final Get-Process found NO live chatterbox servers or Python launcher. Do not assume whether PID 9300 was an independent live server, a crash-related transient, or another condition: investigate. No process was killed for this pause because the invocation had already exited. Idle MSBuild node PID 10492 was seen after completion; no active build remains. Executor 72181 finished exit 1 and must not be resumed.
- models/server.pid is stale and remains for host.kill migration on the next ensure. Current models/build-contract.json matches compiled outputs. EngineBuild removes intermediate build tree on success, retaining build/bin; another source rebuild requires reconfigure. Its contract does NOT hash source files, only CMakeLists.txt/revision/generator/arch. Explicitly invalidate build contract if C++ changes are made; likewise conversion contracts do not hash converter sources, so invalidate relevant stamps and baked contracts when metadata changes without policy changes. Do not silently run stale artifacts.
- Installed .venv stamp still matches requirements and CPU torch settings. ggml pinned HEAD is 7840aaba1989c6deeefede1d77d5aaf8f52b947e. No tests/harnesses were written or run. Net first-party diff versus base 2adbad0 excluding this file was +25 lines at last measurement, BEFORE any later changes (none planned for pause). Requirement allows added help strings but otherwise asks net decrease; measure accurately and reduce duplicate glue if needed, not cosmetic compression.

Remaining Phase F
- [ ] Diagnose nano server exit with actual inference and visible existing server stderr (no new logging/debug prints/log files/harness). Reproduce using exact contract command, respecting single-server ownership. Inspect real GGUF types and graph backend failure. One UNCONFIRMED hypothesis: flow/encoder/*/attn/pos_bias_u and pos_bias_v are 2D arrays and now fall through to q4_0; additive graph operands may require F32. Do not claim this is the cause until observed. Do not add silent dtype fallbacks or CPU dequantization. Fix explicit tensor policy or graph behavior based on evidence.
- [ ] Complete actual nano then turbo then v3 synthesis. Confirm exactly one live server, same-contract PID reuse, model/flag changes unload before starting. Investigate observed two-process snapshot. Leave final server resident, no cleanup on normal tts exit.
- [ ] Exercise --t3-weight-type q8_0, verify conversion/rebake/restart and native stored tensor types. Exercise JSON {"prefix":"model/h0/","type":"f32"} prepended to shipped rules, verify first layer F32 and other matrix layers chosen default. Use real conversion/inference, no test harness/scratch script/golden WAV.
- [ ] Finish metadata/shape audit. Existing S3 CFM stack/mid-block loop counts and some DSP/architecture constants remain; distinguish architectural operations from weight-owned dimensions. CampPlus filter feature dimension remains architectural metadata 80; review strict requirements. No sampling keys may override argv; Llama generation budget remains CLI. No shapes as user flags.
- [ ] Verify default q4 matrices and shipped F32 rules in generated files. quant.py and two shipped JSON files must remain the entire removable quant mechanism. JSON files contain rules; q4 default lives solely in settings table. Unsupported packing throws.
- [ ] Finish first-party line reduction audit relative to 2adbad0, excluding prompt and allowed help strings; no obscuring formatting compression. Requirements unchanged; ggml and sibling untouched. Confirm removed force_f32, siblings, CMAKE_FLAGS, precision JSON and host TerminateProcess. Existing Llama tokenizer child-process TerminateProcess is unrelated and retained.
- [ ] After ALL validation succeeds, rewrite this entire file as a maintenance standing order with empty work checklist; commit using full file as message. Do not mark maintenance/completion prematurely. User explicitly authorizes this checkpoint push and pause; future resume controls continuing work.

Permanent SST invariants
CLI owns seed, temperature, GPT-2 top-k, top-p, repeat-penalty, n-predict, cfm-steps, trim-fade-samples, Llama min-p/cfg-weight/exaggeration/cfm-cfg, gpu; t3/s3 weight type and policy; variant/TEXT/language/reference. Defaults in settings.FLAGS only: seed42, temperature .8, top-k1000, repeat1.2, n-predict1000, trim480, gpu0; GPT-2 top-p .95/cfm-steps2; Llama top-p1/cfm-steps5/min-p .05/cfg-weight .5/exaggeration .5/cfm-cfg .7; types q4_0, shipped policy files, reference.wav. C++ requires exact --names and no defaults. A new flag means one table row, generated help/host pass-through, C++ Flags read, Knobs field and engine use. CMakeLists.txt alone owns build options.
GGUF owns tensor shapes, head/layer/feed-forward/embedding sizes, perceiver/text/wpe layout, tokenizer tokens/IDs/languages, baked tensor lengths and per-tensor stored types. No CLI sampling authority in GGUF. No +4096 converted Llama generation budget. No competing output sample rate metadata.
Quant matching is first-rule-wins with all supplied prefix/suffix/contains/ndim fields conjunctive. Integer arrays never quantized. T3 F32 prefixes chatterbox/, voice_encoder/, model/wpe, model/ln_f/, model/norm/, model/rope_freq_factors; suffix /b,/g; ndim1. S3 F32 prefixes flow/input_embedding, flow/spk_embed_affine/, s3gen/builtin/, s3gen/mel_fb/, campplus/, s3tokv2/, cfm/, hift/; suffix /b; ndim1,3,4. Other matrix tensors use selected default. No Q4_K_M recipe, imatrix, catch/retry/f16 fallback or CPU Q4 dequant. Keep native Vulkan weights and F16 3D conv expansion.

Prohibitions
No tests, pytest, harnesses, CI, log files, debug prints, golden WAVs, scratch scripts, README or extra markdown. No defensive fallbacks, swallowed errors, CPU fallback or silent f16. No pip installs or requirements changes; retain tokenizer language dependencies pykakasi, spacy-pkuseg, dicta-onnx, add-stress-to-epub. Do not add huggingface_hub/scipy/perth/onnxruntime. Torch conversion stays CPU. Do not edit ggml/ or C:\Users\eb-wjt\Downloads\3-way\chatterbox.cpp. No watermark/Perth, --n-gpu-layers, --n-threads, CPU ggml backend or merged GPT-2/Llama engines/bakers/servers. No permission questions for already authorized work. Respect this explicit pause.

Commit ritual
After every phase, including F: read this file in full, rewrite it ENTIRELY from scratch with mission, actual completed/remaining checklist and discoveries. First line is git subject, then blank line and full standing order. Stage implementation and this file; git commit -F PROMPT-BOOTSTRAP-POSTERITY.md. No dummy short body or appended changelog. Continue next phase immediately unless user explicitly requests pause, as now.
