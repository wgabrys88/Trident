# Trident Hand-off — September 2026

## Real North Star (per commit `3015637` extended message)

**Get a live, working `python main.py talk` duplex conversation on Windows that the author can demonstrate on the other computer.**

The commit `3015637` ("Move TTR2 into Chatterbox and split Trident by concern") is the most recent landmark. Its extended message explicitly enumerates the **five remaining items** to be done before "north star done":

1. **Segmenter flushes on every `.?!`.** Counting still becomes "One. jobs." That is the TTFA killer on Iris Xe. Coalesce to 8-20 words before `send_sentence`. (See `generation.py:38-57` `Segmenter.take`.)

2. **VAD start still `preserve_playback=True`.** Playback pauses; Chatterbox keeps synthesizing. GPU stays busy on a cancelled thought. Advance/cancel the native epoch on speech start; resume only if intent is backchannel. (See `talk.py:38-43` `_speech_start`.)

3. **Gemma still has no spoken yield cap that matches the prompt.** `max_tokens 1024` will still dump two minutes of audio. (See `config.py:50` `GEMMA_GEN["max_tokens"] = 1024`.)

4. **16 kHz/24 kHz WASAPI is unproven on hardware.** Last good run was 48 kHz stereo + numpy resample. If a device rejects the mix format even with `auto_convert`, revert only `Wasapi.open` rates/channels; do **not** bring ffmpeg or CABLE back. (See `config.py:142-167` `Wasapi`.)

5. **No live talk verification in this session.** The author tree had no .venv. First check on the other computer: `python main.py install`, then `python main.py talk --family nano --language en`. Solo scripts boot only their resident; `python asr.py` is not a duplex test.

## What was also done in commit `3015637`

- Chatterbox now ships the Windows TTR2 server (origin/main `4275697`).
- Trident clones that pin, cmake-builds `chatterbox-server` with `-DTTS_CPP_BUILD_EXECUTABLES=ON`, copies the exe.
- `tts/CMakeLists.txt` and `tts/src/server.cpp` are gone.
- Python split: `install.py`, `asr.py`, `generation.py`, `tts.py`, `talk.py` — one file per concern, each runnable alone or from `main.py`. `journal.py` stays a library. `runtime.py` stays residents + TTR2 client.
- WASAPI opens at 16 kHz mono capture / 24 kHz mono playback with `auto_convert=True`. The Python `StatefulResampler` and CABLE 48 kHz stereo path are gone. VB-Cable device names still skipped (`config.py:17`).
- ffmpeg dropped from I/O. requirements.txt unchanged: `numpy`, `onnxruntime`, `silero-vad-notorch`, `sounddevice`.

## My North Star was wrong

I was chasing a self-imposed goal ("TTS-to-ASR self-validation of the repeat_penalty fix"). The `repeat_penalty` fix in chatterbox.cpp (commits `1eae6e8` + `aa81669`, pinned in `config.py:14` as `aa81669ebdf0471e51babc8bfc33e8eebc8b8cdf`) is **valid work** — pre-fix R1 P2 inflated 71→429 tokens (6.04×), R4 P3 inflated 66→656 tokens (9.94×), post-fix observed 79→228+45=273 tokens (3.45×). But it was forensic / hygiene work, not the project's real goal.

The validation pipeline I built (files in `tools/`, the chatterbox protocol debugging) is **throwaway scaffolding**. Don't keep it. It is not on the path to the real north star.

## What to actually work on next

1. **Talk command smoke test on the real machine.** `python main.py install` then `python main.py talk --family nano --language en`. Confirm items 4 and 5 above.
2. **Segmenter fix.** Replace the per-punctuation flush with an 8-20 word coalesce in `generation.py:38-57`.
3. **Hard barge-in.** Make `_speech_start` in `talk.py:38-43` actually advance the native epoch and cancel pending chatterbox synthesis, not just pause playback.
4. **Spoken yield cap.** Match `GEMMA_GEN["max_tokens"]` to a budget that fits the prompt's "sixty spoken words" target.
5. **Revert WASAPI rate if needed.** If the 16 kHz/24 kHz devices reject on the target hardware, revert only `Wasapi.open` to 48 kHz/16-bit (do not reintroduce ffmpeg or CABLE).

## Files & commits to read

- `3015637` — "Move TTR2 into Chatterbox and split Trident by concern" (the landmark; full north star in the message)
- `d860a49` / `7959ecf` — "Make talk cutover journals decisive" (VAD playback identity, PCM epoch fields, gemma/TTS ordering, native piece gaps, accepted ASR text)
- `ec2b874` — "Pin Chatterbox 8a66fb0 so a clean clone can build chatterbox-server"
- `1f3ec58` / `5268f96` — "Shrink the Trident stack" (helper consolidation, no behavior change)
- `c15899e` / `e342ba3` — "fix early spoken replies and stop commands" / "tighten duplex talk interruption"

## Files in tree

```
config.py     — knobs, paths, hardware detection, Paths/Wasapi
install.py    — clone pins, build chatterbox-server, fetch models, voice refs
runtime.py    — Residents (parakeet/gemma/chatterbox subprocesses), TTR2 Chatterbox client
tts.py        — Synthesis, Renderer, Sink (WASAPI playback), TTSMode (one-shot)
asr.py        — Capture (VAD + smart-turn), transcribe (parakeet HTTP), Source (WASAPI capture)
talk.py       — Conversation (full duplex: capture→ASR→gemma→TTS→sink)
generation.py — Gemma streaming + Segmenter
journal.py    — Journal (events.jsonl), WorkerSupervisor
main.py       — argparse entrypoint, dispatches install/talk/tts/asr/generation
```

## State of my throwaway work (committed in `b21f339` + `ee28d87`)

These commits are on `runner-x` and pushed. They are not the north star. If the author wants to keep them as a reference, fine; if not, the commits can be reverted without affecting the main path. Files involved:

```
.findings.md
tools/validate_tts_to_asr.py
tools/validate_tts_ffmpeg.py
tools/validate_tts_protocol.py
tools/test_full_validation.txt
tools/test_polish_repeat.txt
tools/list_devices.py
tools/protocol_test.py
tools/proto_test_runner.py
tools/proto_test_delay.py
tools/proto_test_nodelay.py
tools/debug_protocol.py
tools/debug_protocol2.py
tools/validate_*.log
tools/debug_chatterbox.log
tools/proto_chatterbox.log
```

`config.py:14` was changed from `9f30839...` to `aa81669ebdf0471e51babc8bfc33e8eebc8b8cdf` in `b21f339`. The chatterbox repo is already checked out at that SHA. The fix (deduplicated `repeat_penalty` in `t3_mtl.cpp:1142-1147`) is real and correct, just not on the critical path for `talk` mode right now.

## Hardware / model identity (so a new session can verify install)

- Hardware: Pascal (NVIDIA GeForce GTX 1060 6GB, compute 6.1). Vulkan backend, flash_attn on.
- Models in `models/`: `gemma-4-E2B_q4_0-it.gguf`, `chatterbox-t3-mtl-v3-cangjie-q4_0.gguf`, `chatterbox-s3gen-mtl-v3-f16.gguf`, `tdt-0.6b-v3-q4_k.gguf`.
- Ports: parakeet 17931, gemma 17932, chatterbox 17933.
- VB-Audio Virtual Cable is installed but skipped by `wasapi_device()` in `config.py:112-123`. No ffmpeg WASAPI. Default WASAPI devices used.
- Run dir: `data/runs/YYYYMMDD-HHMMSS-fff-{command}-{hardware}-...`. Each run has `run.json` (immutable manifest) and `events.jsonl` (append-only journal).

## Run output that proves the system can run end-to-end

`data/runs/20260901-154635-764881-tts-pascal-v3-pl-trump/` — `python main.py tts --family v3 --language pl` produced a complete event log, 3.45× token ratio (post-fix), `trident.done` exit code 0.
