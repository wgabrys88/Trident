You are continuing work on the Trident project at
C:\Users\px-wjt\Downloads\Trident on branch runner-x. Before you
touch any code you MUST do all of the following, in order. Do not
skip steps. Do not edit anything until the reading and the system
runs are done.

NORTH STAR

User talks over the machine. Sound stops. Only the new reply is
heard. First piece of audio starts while Gemma is still generating.
The GPU does not idle between TTS pieces. The assistant's own
voice is never heard as a user turn. Prove it on the real rig
before claiming it.

WHAT THIS SESSION MUST DO

You are to:

1. Read every tracked file on runner-x (the .gitignore in this
   repo whitelists a fixed list of files and directories; only
   those are tracked). Read every one of them, top to bottom,
   without skipping.

2. Run the system end to end on the real rig (Windows x64, GTX
   1060 6 GB). Run the three diagnostic gates in this exact order:

       .\.venv\Scripts\python.exe main.py install
       .\.venv\Scripts\python.exe main.py resident boot --family turbo --tts-language en
       .\.venv\Scripts\python.exe main.py vram-audit
       .\.venv\Scripts\python.exe main.py agent --duration-seconds 120
       .\.venv\Scripts\python.exe main.py resident stop

   Note the failure mode at each step. If any gate fails, do not
   move on. Resolve it before moving on.

3. After running, study the run logs under data/runs/ and the
   git history:

       git log --oneline runner-x
       git show <commit> -- <file>

   Read every commit message in full. f358019 on runner-x and
   68c6926 on wgabrys88/chatterbox.cpp are the anchor commits
   that the prior session claims to have earned. Treat the prior
   commit messages as claims, not as ground truth. Verify each
   claim against the actual code and the actual run logs.

4. Whenever you find a defect, trace it to its real root cause.
   Do not patch symptoms. The defects that the prior session
   already identified and that this session must confirm are:

   a. local_api.py line 270. chatterbox_stream opens the WAV
      with wave.open(str(output), "wb") without first creating
      the parent directory. Triggers for any caller that hands
      a fresh subdirectory path (for example agent.py:188-189
      passing paths.run_dir / ".actor" / "actor-NN.wav").

   b. conversation.py lines 496-545. _tts_loop calls
      client.open(wav_path), client.cancel_piece(), and
      client.end() against ChatterboxClient, which provides
      only open() with no argument, cancel(), and close(). At
      runtime this raises TypeError on every piece, so the
      conversation engine cannot speak. The f358019 commit
      message claimed a 10-turn agent ran end to end. The
      claimed run did not happen with this code. Re-verify by
      reading the code, not the message.

   c. conversation.py. Capture.submit_audio is defined twice
      (around lines 82 and 232). Python rebinds; only the second
      runs. Functionally fine but it is dead code. Strip the
      duplicate.

   These three defects together are why the 120s agent crashed
   at the first actor clause with a FileNotFoundError on
   data/runs/<id>-agent/.actor/actor-00.wav. Fixing (a) alone
   unmasks (b). Fix both before the agent can speak.

5. Now that you have the truth, derive your own plan. Do not
   take any prior session summary at face value. The plans and
   status notes left in the working tree and in commit messages
   are aspirational. Reconstruct ground truth from the code and
   from the rig runs.

HOW TO FIX A DEFECT CORRECTLY

When you find a problem, do this:

a. Read the failing site in full. Read all of its callers. Read
   everything that the failing site imports. Understand the
   contract.

b. Trace the failure to a root cause. State the root cause in
   one sentence. State why the current code violates the
   contract in another sentence.

c. Write the minimal fix that satisfies the contract. No
   comments. No defensive guards "just in case". No related
   cleanup.

d. Run the system on the rig and verify the original failure
   is gone and nothing else broke. The diagnostic gates are the
   only verification that counts.

e. Stage and commit one logical change with an extended commit
   message that includes: the root cause, what was lost, what was
   left, and what the next session should read first.

f. Push immediately. Never leave unpushed commits when pausing.

RULES (NON-NEGOTIABLE)

- Real rig runs only. No harnesses, mocks, simulators, or test
  scaffolding added to the repo.
- Define success before editing. State what "fixed" means.
- Evidence in fixed order: real rig runs, current source, git
  history, working memory last.
- Root defect, trigger, symptom, escape condition stay separate.
- Always push for code reduction. Delete duplication. No
  abstractions over consolidation. No dead branches, no unused
  compat, no bookkeeping. No comments. No new dependencies.
- Architectural change is pre-approved by the pinned
  north star; preserve nothing for its own sake.
- Do not patch the chatterbox.cpp pin locally and ship a
  Trident-only workaround. Open a PR upstream.

WHAT TO STAY AWAY FROM UNTIL THE THREE GATES PASS

- UI work. The Gradio modernization, the live ASR tab, the
  voice cloning UX: not until speech actually works on the
  rig through the conversation engine end to end.
- Long-term memory between sessions: not until gates pass.
- Multiphase T3 overlap: not until gates pass; open a PR
  upstream on the pin.
- Privacy and network audit hardening: not until gates pass.
- Stripping knobs from config.py or constants from
  tts/include/audio.hpp and tts/src/audio.cpp without proof
  they are unused in this revision. ECHO_RING_MS is still
  referenced from conversation.py. The TTS_FIELDS stream knobs
  are still parsed by the C++ CLI. kGlue and kQuietAmp2 are
  still referenced from audio.hpp and session.cpp.

DO NOT TOUCH

- The pinned dependency surface. Gradio 6.26.0. silero-vad-
  notorch. onnxruntime. sounddevice.
- SOURCES chatterbox url. The installer already fetches via
  refs/heads/main, not a SHA, so the fetch plus reset does not
  wipe local pin changes.
- The wire protocol on the TTS socket. The Triton TTS wire
  is correct: client sends u32 piece_count then u32 text_len,
  text per piece; resident replies with u32 kind, u32 bytes,
  payload where kind=2 is int16 PCM at 24 kHz, kind=0 is
  metrics, kind=1 is error.
- The cleaned-up CLI / server / session / audio code under
  tts/ that shipped in f358019. Read it before touching it.

SELF-CHECK BEFORE YOU DECLARE DONE

Before you push a "fix" commit, answer yes to each:

- Did I run the system on the rig after the change and observe
  at least one full agent turn end to end?
- Did I read the run log and confirm it matches what the commit
  message claims?
- Did I trace the failure to the actual code path, not to a
  symptom?
- Is the commit free of comments, dead branches, and new
  dependencies?
- Does the working tree only contain the intended files?
- Is the commit pushed?

If any answer is no, do not push. Fix it first.

WHEN YOU PAUSE

- Stop all residents (python main.py resident stop) before
  pausing.
- Make sure the working tree contains only intended changes.
  data/handover-prompt.txt is on disk but is not tracked.
- Push every commit.
- Leave a brief note in the run log
  (data/runs/<latest>/trident.log) describing what was just
  earned and what is left, so the next agent has a pointer.

START NOW. Read every tracked file. Run the gates. Find the
truth. Only then touch code.
