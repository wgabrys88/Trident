diff --git a/scripts/convert-s3gen-to-gguf.py b/scripts/convert-s3gen-to-gguf.py
index 3b0be79..73b421f 100644
--- a/scripts/convert-s3gen-to-gguf.py
+++ b/scripts/convert-s3gen-to-gguf.py
@@ -58,9 +58,10 @@ VARIANTS = {
     },
     "mtl": {
         "repo_id": MTL_REPO_ID,
-        "allow_patterns": ["ve.pt", "t3_mtl23ls_v2.safetensors", "s3gen.pt",
+        "revision": "5bb1f6ee58e50c3b8d408bc82a6d3740c2db6e18",
+        "allow_patterns": ["ve.pt", "t3_mtl23ls_v3.safetensors", "s3gen_v3.pt",
                            "grapheme_mtl_merged_expanded_v1.json", "conds.pt", "Cangjie5_TC.json"],
-        "ckpt_filename": "s3gen.pt",
+        "ckpt_filename": "s3gen_v3.pt",
         "loader": "torch",
         "gguf_name": "Chatterbox Multilingual S3Gen",
         "gguf_description": "S3Gen standard-CFM (10-step Euler, CFG) + HiFT vocoder for ggml port.",
@@ -244,6 +245,7 @@ def main():
     else:
         ckpt_dir = Path(snapshot_download(
             repo_id=cfg["repo_id"], token=args.hf_token,
+            revision=cfg.get("revision"),
             allow_patterns=cfg["allow_patterns"],
         ))
     args.out.parent.mkdir(parents=True, exist_ok=True)
@@ -528,0 +531,3 @@ def main():
+        mel_fb_16k_128 = librosa.filters.mel(sr=16000, n_fft=400, n_mels=128, fmin=0, fmax=8000).astype(np.float32)
+        add_tensor_maybe_q(writer, "s3tokv2/mel_fb", np.ascontiguousarray(mel_fb_16k_128), args.quant, stats=qstats)
+        print(f"Embedded S3TokenizerV2 mel_fb {mel_fb_16k_128.shape}")
diff --git a/scripts/convert-t3-mtl-to-gguf.py b/scripts/convert-t3-mtl-to-gguf.py
index 182a6ba..573c2e3 100644
--- a/scripts/convert-t3-mtl-to-gguf.py
+++ b/scripts/convert-t3-mtl-to-gguf.py
@@ -43,10 +43,11 @@ _SHOULD_QUANTIZE, _RQ_QUANT_TYPE = _load_requantize_policy()
 
 
 REPO_ID = "ResembleAI/chatterbox"
+REVISION = "5bb1f6ee58e50c3b8d408bc82a6d3740c2db6e18"
 ALLOW_PATTERNS = [
     "ve.pt",
-    "t3_mtl23ls_v2.safetensors",
-    "s3gen.pt",
+    "t3_mtl23ls_v3.safetensors",
+    "s3gen_v3.pt",
     "grapheme_mtl_merged_expanded_v1.json",
     "conds.pt",
     "Cangjie5_TC.json",
@@ -333,13 +334,14 @@ def main() -> None:
     else:
         ckpt_dir = Path(snapshot_download(
             repo_id=REPO_ID,
+            revision=REVISION,
             token=args.hf_token or os.getenv("HF_TOKEN"),
             allow_patterns=ALLOW_PATTERNS,
         ))
     args.out.parent.mkdir(parents=True, exist_ok=True)
 
     print(f"Loading checkpoint from {ckpt_dir}")
-    state = load_file(ckpt_dir / "t3_mtl23ls_v2.safetensors")
+    state = load_file(ckpt_dir / "t3_mtl23ls_v3.safetensors")
     if "model" in state and not torch.is_tensor(state["model"]):
         state = state["model"][0]
     conds = torch.load(ckpt_dir / "conds.pt", map_location="cpu", weights_only=True)
